{
	"name": "@plumb/web",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build && vite build --ssr src/serve.ts --outDir .local/server && node .local/server/serve.js",
		"preview": "vite preview",
		"test": "vitest run"
	},
	"dependencies": {
		"@perish/design": "0.2.1",
		"svelte": "catalog:"
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "catalog:",
		"@types/node": "catalog:",
		"sass": "catalog:",
		"typescript": "catalog:",
		"vite": "catalog:",
		"vitest": "catalog:",
		"wrangler": "catalog:"
	}
}
