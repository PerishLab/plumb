use super::super::rule;

rule!(
    GUARD_CHECKS_RELEASE_PROFILE,
    "structure.guard-checks-release-profile"
);
rule!(GUARD_CONCURRENCY, "structure.guard-concurrency");
rule!(GUARD_RUNS_DOCTOR, "structure.guard-runs-doctor");
rule!(GUARD_RUNS_ECTROPY, "structure.guard-runs-ectropy");
rule!(
    GUARD_USES_CURRENT_ECTROPY_MODE,
    "structure.guard-uses-current-ectropy-mode"
);
