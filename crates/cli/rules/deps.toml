# The deps dimension's self-built policy. Self-built packages live under this
# scope and track latest-stable — a version-pinned specifier is out of true.
# Retired names must migrate to their replacement.

scope = "@perish"

[[retired]]
name = "@perish/harness"
use = "@perish/sealkit"
