# The structure dimension's membership sets. Repaying an unknown-shape debt is a
# row added here, not a function rewritten. Whitelists of legal names; a name in
# none of them is unknown shape.

dirs = [
  "app",
  "apps",
  "charts",
  "clusters",
  "crates",
  "deploy",
  "dist",
  "docs",
  "examples",
  "hosts",
  "lib",
  "packages",
  "repos",
  "runner-control",
  "skills",
  "tests",
]
lanes = ["deploy", "guard", "release-binary", "release-cargo", "release-exact", "release-runner", "release-stable", "probe"]
