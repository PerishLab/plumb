# Migration

No caller migration is required. Repositories do not acquire permission to add
arbitrary control directories or workflow lanes; only `runner-control` and
`release-runner` have entered the living skeleton.
