# Plumb v0.18.22

The living Actions skeleton now admits the `runner-control` directory and the
`release-runner` workflow lane. These are exercised by the shared Actions
repository's self-hosted runner release transaction; they are no longer
reported as unknown shape.

No release, document, site, or package contract changes in this release.
