# Migrating to Plumb v0.25.0

## Nothing to do

An image that wraps a binary gains a second way to find its payload and keeps the
first. A repository whose lane holds the archive behaves exactly as before.

A repository projecting through the rendered ship lane could not carry such an
image at all before this release; it can now, and needs no declaration to say so.
