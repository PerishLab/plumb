# Plumb v0.18.14

## Cargo 耦合包族的预发布版本

Cargo 发布标记现在会把 `release.cargo.packages` 声明的包视为同一个耦合包族。
选择精确预发布版本时，Plumb 会在 Cargo 预演或发布前同步更新包版本，以及内部
路径依赖的精确版本要求。

改写范围覆盖工作区依赖，以及包级普通依赖、构建依赖和开发依赖；同时仍严格限制为
包身份属于已声明发布包族的路径依赖。
