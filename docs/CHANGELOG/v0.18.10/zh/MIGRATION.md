# 迁移到 Plumb v0.18.10

本次发布不需要任何仓库迁移。

仍携带通用 `land` 包装器的仓库可以删除它并直接调用 `plumb land`。包装器不会被自动
移除，Doctor 目前也不会报告它的存在。

协调面可以用仓库路径调用 `plumb::land::run`。它从 `origin` 解析 Forgejo 远端，经既有
配置级联与 Tea 登录席位读取令牌，并报告 PR、投影与已同步的基线席位。
